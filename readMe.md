# Booking Application With CLI

This is a simple booking application that allows users to book tickets for various events through a command-line interface (CLI). The application supports user registration, event listing, ticket booking, and viewing booked tickets.
